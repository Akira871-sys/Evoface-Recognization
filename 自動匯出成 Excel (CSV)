import pandas as pd # 需要先 pip install pandas

def export_to_csv():
    conn = sqlite3.connect('attendance.db')
    # 使用 pandas 直接讀取 SQL 並轉成 DataFrame
    df = pd.read_sql_query("SELECT * FROM logs ORDER BY timestamp DESC", conn)
    
    filename = "attendance_report.csv"
    df.to_csv(filename, index=False, encoding='utf-8-sig') # utf-8-sig 解決 Excel 中文亂碼
    
    print(f"✅ 報表已匯出至: {filename}")
    conn.close()
